See [AGENTS.md](AGENTS.md) for project instructions. This file is a pointer so
Claude Code picks up the same content as other coding agents (Codex, Cursor,
Aider, etc.). Machine-global rules live in the shared agent-shared baseline
([global/AGENTS.md](global/AGENTS.md)), imported via `~/.claude/CLAUDE.md`; run
[install.sh](install.sh) once per machine to wire that up.
